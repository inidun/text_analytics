[pytest]

norecursedirs = .git data output deprecated __pycache__ notebooks
addopts = --disable-warnings --show-capture=all -s
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
